# Pinned post

Quote-post the X Article. `{ARTICLE_URL}` is the article's URL once published; X renders the quote card, so the URL itself can sit at the end of the text. Attach nothing else if the quote card shows; if the article is posted as a plain link instead, attach `visuals/01-loop.png`.

## pinned (266 / 280 with the url counted as 23)

```
coins that build apps. every fee burns PYRE.

write one sentence. a coin launches on pons v2. its creator fees pay an agent to build the app. the app is free. 25% of every fee buys PYRE and burns it, attested on chain.

how it works, in full: {ARTICLE_URL}
```

## alternate a (271 / 280)

```
pyre is live on robinhood chain.

a coin's trading fees pay an ai agent to build the app the coin describes. the app is free. a quarter of every fee buys PYRE and burns it. every burn carries a hash you can check on blockscout.

the full write-up: {ARTICLE_URL}
```

## alternate b (268 / 280)

```
fees fund the build. fees fund the burn.

pons v2 launch → 70% of the 1% fee to the app → 60% pays an agent to ship it behind a build gate → 25% buys PYRE and burns it → 15% to the launcher. the app is free. open source, mit.

how it works: {ARTICLE_URL}
```

Replace with the $PYRE launch post once the coin is live (BRAND.md §6). Until then this stays pinned.
